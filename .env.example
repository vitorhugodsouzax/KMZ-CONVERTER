# O servidor usa variáveis de ambiente do processo. Node 22+ pode carregar com --env-file.
PORT=3000
HOST=127.0.0.1
# Opcional: caminho do Rscript quando o R não estiver no local padrão usado pelo projeto.
# RSCRIPT_PATH=C:\Program Files\R\R-4.6.1\bin\Rscript.exe
# Use somente em testes/diagnóstico para pular a busca CNEFE.
# GEOCODING_DISABLED=1
